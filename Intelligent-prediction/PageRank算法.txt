// 运行PageRank算法，找出最有影响力的20个参与方
CALL gds.pageRank.stream('fullGraph')
YIELD nodeId, score
WITH gds.util.asNode(nodeId) AS node, score
WHERE 'Participant' IN labels(node)
RETURN node.`参与方名称` AS 参与方, score
ORDER BY score DESC
LIMIT 20